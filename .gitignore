# Logs
logs
*.log
npm-debug.log*
node_modules
dist
.DS_Store

# Environment variables (local secrets/config - never commit these)
.env
.env.local
.env.*.local

# RAG ingestion (Step 2): local PDFs + resume state - not committed
scripts/lectures/
scripts/.ingest-progress.json
