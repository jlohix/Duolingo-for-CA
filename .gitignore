# Logs
logs
*.log
npm-debug.log*
node_modules
dist
.DS_Store

# Environment variables (local secrets/config - never commit these)
.env
.env.local
.env.*.local


# Local dev-only module-attempt logger (Option B) — never commit
logger-server.mjs
module_attempts.csv
check-attempts.mjs
